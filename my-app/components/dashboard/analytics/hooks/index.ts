// Barrel exports for analytics hooks
export { useAnalytics, useQuickAccess } from './useAnalytics';

// Re-export types that hooks might need
export type { 
  AnalyticsData, 
  TimePeriod,
  SalesData,
  PaymentMethod,
  CartData,
  ShopActivity,
  Customer,
  QuickAccessItem
} from '../types'; 